/**
 * Exam configuration — copy to js/config.js and set your values.
 * js/config.js is gitignored so secrets are not pushed to GitHub.
 */
window.EXAM_CONFIG = {
  /** Vercel grading API URL (deploy api/ folder to Vercel) */
  GRADE_API_URL: 'https://YOUR_PROJECT.vercel.app/api/grade',

  /** Require students to enter an access code before starting (matches EXAM_ACCESS_CODE on Vercel) */
  REQUIRE_EXAM_CODE: true
};
