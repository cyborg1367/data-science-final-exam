# Vercel environment variables (set in dashboard — do not commit real values)
EXAM_ACCESS_CODE=change-me-before-exam
ALLOW_GITHUB_PAGES=true
# ALLOWED_ORIGINS=https://yourname.github.io
# ANSWER_KEY_JSON={"passThreshold":60,...}  # or deploy api/data/answer-key.json via CLI
